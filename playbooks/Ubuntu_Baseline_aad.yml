---
- name: Configure baseline policy on Ubuntu with PAM Authentication and Azure Active Directory (Entra ID)
  hosts: all
  become: true
  roles:
    - password_aad
    - session_timeout
    - ssh_banner
    - tls_ciphers
    - elastic_agent
